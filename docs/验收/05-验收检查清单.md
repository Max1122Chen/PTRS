# 05 — 验收检查清单

答辩前请逐项勾选。命令均在**项目根目录**执行（前端命令在 `frontend/`）。

---

## A. 环境与构建

- [ ] JDK 21、`mvn -v` 正常
- [ ] `mvn test` 全绿
- [ ] `cd frontend && npm run build` 成功
- [ ] `python scripts/validate_s2_closure.py` 8/8 OK

---

## B. 运行

- [ ] `mvn spring-boot:run "-Dspring-boot.run.profiles=dev"` 启动无致命错误
- [ ] `npm run dev` 后 <http://localhost:5173> 可访问
- [ ] **无 MySQL** 下登录、推荐、景区页可正常使用

---

## C. 主功能演示（参见 [02-答辩演示脚本.md](02-答辩演示脚本.md)）

- [ ] 游览 → 推荐 → 进入景区工作台（`areaId=252`）
- [ ] 地图点 POI 设锚点 → 设施查询 + 地图高亮
- [ ] 美食 Tab 有数据且含距离
- [ ] 两点 / 多点路线规划（含失败时的不可达提示）
- [ ] 室内：双击 `indoorAvailable` POI → 楼层切换 → 室内规划
- [ ] 日记列表与检索
- [ ] （可选）`dev_admin` 管理页

---

## D. 创新与课程约束（可口述 + 指代码）

- [ ] 能说明 Dijkstra / TSP / TopK / Trie / NGram 落点（见 [03-核心算法说明.md](03-核心算法说明.md)）
- [ ] 能说明检索不用 SQL LIKE 做计算
- [ ] 能说明 `com.travel.ds` 使用范围（见 Tech 文档）

---

## E. 待现场完成

- [ ] **FR-009-5**：AIGC 动画实机出片 ≥1 条（需密钥）
- [ ] 答辩 PPT 含 FR 裁剪说明（004-4 / 015 / 016）
- [ ] （可选）录屏备份演示视频

---

## F. 已知搁置 / 风险（主动说明）

- [x] **S2-R8** 沙河室内人工校正：S4区（900022208）、学术报告厅（900022232）可演示；图书馆/公共教学楼无室内入口
- [ ] OSM 采集脚本设施分流与 FR-006 语义仍可继续优化（不阻塞答辩）
- [ ] DB 密码明文 — 仅 dev，答辩环境说明即可

---

## 快速回归命令（复制执行）

```powershell
python scripts/validate_s2_closure.py
mvn test
cd frontend; npm run build
```

**完成日期**：________　**检查人**：________
